// Basic test example
describe('addypowerwash2 API', () => {
  test('should define a basic test', () => {
    expect(true).toBe(true);
  });

  test('addition works correctly', () => {
    expect(1 + 1).toBe(2);
  });
});
